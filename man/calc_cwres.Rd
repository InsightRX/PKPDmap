% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calc_cwres.R
\name{calc_cwres}
\alias{calc_cwres}
\title{Calculate CWRES (Conditional Weighted Residuals)}
\usage{
calc_cwres(
  eta_hat,
  ipred_raw,
  y,
  omega_full,
  error,
  obs_type,
  transf,
  model,
  parameters_population,
  nonfixed,
  as_eta,
  covariates,
  regimen,
  lagtime,
  t_obs,
  obs_type_sim,
  int_step_size = 0.1,
  iov_bins = NULL,
  A_init = NULL,
  t_init = 0,
  steady_state_analytic = NULL,
  weight_prior_var = 1,
  delta = 1e-04
)
}
\arguments{
\item{eta_hat}{numeric vector of estimated etas (MAP estimates)}

\item{ipred_raw}{numeric vector of raw (untransformed) individual
predictions at eta_hat}

\item{y}{numeric vector of observed values}

\item{omega_full}{full omega covariance matrix}

\item{error}{list with `prop` and `add` residual error components}

\item{obs_type}{integer vector of observation types}

\item{transf}{transformation function (identity or log for LTBS)}

\item{model}{PKPDsim model object}

\item{parameters_population}{list of population parameters}

\item{nonfixed}{character vector of non-fixed parameter names}

\item{as_eta}{character vector of parameters estimated directly as eta}

\item{covariates}{list of PKPDsim covariates}

\item{regimen}{PKPDsim regimen object}

\item{lagtime}{lagtime specification}

\item{t_obs}{numeric vector of observation times}

\item{obs_type_sim}{observation type vector for simulation}

\item{int_step_size}{integrator step size}

\item{iov_bins}{IOV bin specification}

\item{A_init}{initial state vector}

\item{t_init}{initialization time}

\item{steady_state_analytic}{steady state settings (or NULL)}

\item{weight_prior_var}{prior weight variance scaling factor (default 1).
Used to scale omega in the FOCE Hessian computation to match the
estimation objective. CWRES uses the unscaled omega (model diagnostic).}

\item{delta}{perturbation size for finite differences}
}
\value{
list with components:
  \item{cwres}{numeric vector of CWRES values}
  \item{vcov}{FOCE variance-covariance matrix of eta estimates
    (n_eta x n_eta), or NULL if computation failed. Derived from the
    same Jacobian F used for CWRES: vcov = (Omega^-1 + F' Sigma^-1 F)^-1}
}
\description{
Computes CWRES following the FOCE approximation method described in
Hooker et al. (2007). The Jacobian of model predictions with respect to
random effects (etas) is computed via central finite differences.
}
\details{
CWRES = L^{-1} * (y - f(eta_hat) + F * eta_hat)

where:
- f(eta_hat) = individual predictions (IPRED) in the transformed space
- F = Jacobian df/deta evaluated at eta_hat
- L * L^T = V = F * Omega * F^T + Sigma (FOCE variance)
- Sigma = diagonal residual error variance matrix
}
\references{
Hooker AC, Staatz CE, Karlsson MO. Conditional weighted
  residuals (CWRES): a model diagnostic for the FOCE method.
  Pharm Res. 2007;24(12):2187-2197.
}
