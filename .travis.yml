language: R
sudo: required
warnings_are_errors: false

r_packages:
  - testit
  - devtools
  - covr

r_github_packages:
  - ronkeizer/PKPDsim

after_success:
  - Rscript -e 'library(covr); codecov()'
